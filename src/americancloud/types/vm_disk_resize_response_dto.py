# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata


class VmDiskResizeResponseDto(UniversalBaseModel):
    id: str = pydantic.Field()
    """
    Virtual machine ID.
    """

    status: str = pydantic.Field()
    """
    Transitional state during the resize.
    """

    reboot_initiated: typing_extensions.Annotated[
        bool,
        FieldMetadata(alias="rebootInitiated"),
        pydantic.Field(
            alias="rebootInitiated",
            description="True when the VM was running and `reboot=true`, so the new disk size was applied via a reboot. False otherwise (VM was already stopped, or `reboot=false`, or the reboot step failed — see `message`).",
        ),
    ]
    message: typing.Optional[str] = pydantic.Field(default=None)
    """
    Present only when the disk resize succeeded but the follow-up reboot failed; surfaces a remediation hint to the caller.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
